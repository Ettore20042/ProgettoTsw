const cardImagesViewport = document.querySelector('.product-card_images');
const slidesTrack = document.querySelector('.product-card_slides-track');
const slidesImages = Array.from(slidesTrack.children);